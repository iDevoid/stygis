package state

const (
	// HandlerUserKeyCookie is the key for setting up the cookie for logged in user
	HandlerUserKeyCookie = "unique_key_value"

	// HandlerKeyEmail is the key of http rest handler to get the email from FormValue
	HandlerKeyEmail = "email"
	// HandlerKeyPassword is the key of http rest handler to get the password from FormValue
	HandlerKeyPassword = "password"
	// HandlerKeyUsername is the key of http rest handler to get the username from FormValue
	HandlerKeyUsername = "username"
	// HandlerKeyUserID is the key of http rest handler to get the userID from FormValue
	HandlerKeyUserID = "user_id"
)
